[app]
name = "velontri"
port = 3000

[build]
install = "cd frontend && npm install --prefer-offline"
build   = "cd frontend && npm run build && cp -r public .next/standalone/public && cp -r .next/static .next/standalone/.next/static"
start   = "cd frontend && node .next/standalone/server.js"
